package com.teleport.candidate_assessment.utils;

/** The type Constants. */
public class TaskManagerConstant {
  /** The enum Status. */
  public enum Status {
    /** New status. */
    NEW,
    /** In progress status. */
    IN_PROGRESS,
    /** Completed status. */
    COMPLETED
  }

  /** The enum Priority. */
  public enum Priority {
    /** Low priority. */
    LOW,
    /** Medium priority. */
    MEDIUM,
    /** High priority. */
    HIGH
  }
}
